import ChatWindow from '../components/ChatWindow';

const Home = () => {
    return (
        <main className="overflow-hidden w-full h-screen relative">
            <ChatWindow />
        </main>
    );
};

export default Home;
