import ChatWindow from '../components/ChatWindow';

const Home = () => {
    return (
        <main className="bg-gray-800">
            <ChatWindow />
        </main>
    );
};

export default Home;
